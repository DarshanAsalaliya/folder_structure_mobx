import 'package:flutter/material.dart';

class ProjectFilledButton extends StatelessWidget {
  const ProjectFilledButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}